const { useRouter } = require("next/router");

function CientProjectForSelectedClient(){
    //There is a file and a folder with a dynamic segment

    //Now the router.query in this case will give back the concrete rvalues for both of the folder routes
    const router = useRouter()
}
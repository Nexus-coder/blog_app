export default function ProjectPage() {
    return (
        <main>
            <h1>Hello Andrew</h1>
        </main>
    )
}

// //This page is generated only when you make a new request to the server
// export async function getServerSideProps(context) {
//     //the request and response here are the default response and request objects in node jss
//     const { params, req, res } = context;
//     //This function has some extra implications being that it gets all the whole reqquest object and even the response object
// }
